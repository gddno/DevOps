package main

import "fmt"

func main(){
	var location = "enter"
	if location == "mount"{ 
		fmt.Printf("Эта строка повествует о %v", location)
	} else if location == "cave" {
		fmt.Printf("Эта строка повествует о %v", location)
	} else {
		fmt.Printf("Эта строка повествует о %v", location)
	}
}